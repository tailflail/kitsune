<%= form_for current_user.active_relationships.build do |form| %>
  <%= hidden_field_tag :followed_id, user.id %>
  <%= form.submit "+ Follow", class: "cursor-pointer p-1 lg:p-2 border-2 rounded-lg border-green-200 text-green-200 hover:bg-green-200 hover:text-green-900" %>
<% end %>