<%= form_for current_user.active_relationships.build do |form| %>
  <%= hidden_field_tag :followed_id, user.id %>
  <%= form.submit "Follow", class: "cursor-pointer px-3 py-2 border-2 rounded-lg border-blue-800 text-blue-600 hover:bg-blue-800 hover:text-neutral-200" %>
<% end %>